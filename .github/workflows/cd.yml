#name: "CD (Entrega/Delivery Continua): Main workflow"

#cirar valores secretos em github settings - Secrets and variabels/ actions
#env:
  #VERCEL_TOKEN: ${{secrets.NomeDaVariavel}}
  #VERCEL_ORG_ID: ${{secrets.NomeDaVariavel}}
  #VERCEL_PROJECT_ID: ${{secrets.NomeDaVariavel}}

#on:
  #push:
    #branches: ["main"]

#jobs:
  #deploy:
    #runs-on: ubuntu-latest
    #steps:
      #- uses: actions/checkout@v3

      #- name: Run npm install
        #run: npm install

      #- name: Run Deploy
       #run: npm run deploy:prod